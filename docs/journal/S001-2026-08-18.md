# Phiên S001 — 2026-08-18

**Mục tiêu phiên:** khảo sát hiện trạng, chốt kế hoạch tái cấu trúc toàn diện với
người dùng, dựng hạ tầng nối tiếp phiên, rồi bắt đầu Phase 1.

---

### 11:20 — Khảo sát hiện trạng

**Bối cảnh:** phiên đầu tiên, chưa biết gì về dự án ngoài README.

**Đã làm:** đọc README, 2 file `.csproj`, `build.ps1`, `app.manifest`; liệt kê
namespace của toàn bộ 9 file `.cs`; grep tìm các kiểu được tham chiếu.

**Kết quả — phát hiện quan trọng:** README **mô tả sai** hiện trạng. Cụ thể:

- README vẽ layout `src/Tsudev.Audit.Core|Windows|Cli` + `tests/unittests`,
  nhưng thực tế cả 9 file `.cs` nằm phẳng ở thư mục gốc.
- `Tsudev.Audit.Core.csproj` **không tồn tại**, dù cả hai csproj còn lại đều
  `ProjectReference` tới nó → không thể build.
- Namespace `Tsudev.Audit.Core.Rendering` **không tồn tại trong bất kỳ file nào**,
  nhưng `Program.cs:86,87,118` gọi `HtmlReportRenderer`, `XlsxWriter`,
  `DashboardBuilder`. Toàn bộ lớp render báo cáo bị thiếu.
- Không có `.sln`, không có `nuget.config`, chưa `git init`.
- README khẳng định "54 test chạy trên Linux CI" — **không thể đúng**, vì không
  có `unittests.csproj`; file test bị lạc ở `mnt/user-data/outputs/…`.

**Kết luận:** dự án **không build được**. Phải khôi phục trước khi bàn tới
installer / chữ ký số.

**Quyết định & lý do:** báo cáo thẳng sự lệch giữa README và thực tế trước khi
đề xuất bất cứ điều gì. Nếu im lặng làm theo README, mọi ước lượng khối lượng
công việc sau đó đều sai.

**Tiếp theo:** hỏi người dùng 4 quyết định định hình kế hoạch.

---

### 11:35 — Chốt 4 quyết định với người dùng

**Kết quả:**

1. **Rendering:** viết mới từ đầu (không có bản gốc để khôi phục). XLSX tự ghi
   OpenXML, không thêm phụ thuộc NuGet.
2. **Đóng gói:** Inno Setup EXE **và** portable single-exe + winget.
   Loại bỏ MSI/WiX và MSIX.
3. **Ký số:** Azure Trusted Signing.
4. **License:** *không* làm hệ thống bản quyền cho tool; "license" chỉ là tính
   năng kiểm tra bản quyền Windows/Office đã có sẵn.

Người dùng bổ sung thêm một yêu cầu quan trọng: **phải có hệ thống file markdown
ghi log/phiên/nhật ký** để phiên sau tiếp tục được khi máy sập hoặc hết context.

---

### 11:40 — Môi trường dev

**Đã làm:** kiểm tra `dotnet` → không có. Kiểm tra mạng → có internet.
Cài .NET SDK vào `~/.dotnet` bằng `dotnet-install.sh` (không cần root).

**Kết quả:** `dotnet 8.0.424` chạy được. Xác nhận có thể build **và** publish
`win-x64` self-contained ngay từ Linux (`net8.0-windows` không dùng WinForms/WPF
nên cross-compile được). Chỉ **không chạy thử** được file exe ở đây.

**Quyết định & lý do:** cài vào `~/.dotnet` thay vì cài hệ thống — tránh cần
`sudo` và không đụng vào máy người dùng.

---

### 11:45 — Phase 0: hạ tầng nối tiếp phiên ✅

**Đã làm:** tạo `docs/STATE.md`, `docs/CONTINUITY.md`, `docs/PLAN.md`,
`docs/journal/S001-2026-08-18.md` (file này).

**Quyết định & lý do:** làm việc này **trước** khi động vào mã nguồn. Sắp tới là
một đợt tái cấu trúc di chuyển hàng loạt file — nếu đứt giữa chừng mà không có
bản ghi, phiên sau sẽ đứng trước một thư mục nửa vời và không biết đó là do
thiết kế hay do sự cố.

**Tiếp theo:** Phase 1 — `git init`, rồi tái cấu trúc sang layout `src/`.

---

### 12:05 — Phase 1 hoàn tất: khôi phục khả năng build ✅

**Bối cảnh:** dự án không build được do layout phẳng + thiếu project file.

**Đã làm:**

- `git init` + `.gitignore`, commit `d9bb51b` snapshot nguyên trạng **trước khi**
  di chuyển bất cứ file nào — đây là điểm khôi phục nếu tái cấu trúc hỏng.
- `git mv` toàn bộ 9 file `.cs` vào đúng layout mà các `.csproj` đang trỏ tới:
  `src/Tsudev.Audit.Core/{Models,Abstractions,Collectors,Reports,Testing}/`,
  `src/Tsudev.Audit.Windows/`, `src/Tsudev.Audit.Cli/`.
- Cứu `tests/unittests/Program.cs` khỏi `mnt/user-data/outputs/…`, xoá cây `mnt/`.
- Tạo mới: `Tsudev.Audit.Core.csproj`, `tests/unittests/unittests.csproj`,
  `Tsudev.SystemAudit.sln`, `Directory.Build.props`.

**Kết quả — đúng như dự đoán, chỉ còn thiếu Rendering:**

```
src/Tsudev.Audit.Cli/Program.cs(7,25):  error CS0234: 'Rendering' does not exist
src/Tsudev.Audit.Cli/Program.cs(145,62): error CS0246: 'HtmlReportRenderer' not found
src/Tsudev.Audit.Cli/Program.cs(146,9):  error CS0246: 'XlsxWriter' not found
tests/unittests/Program.cs(8,25):        error CS0234: 'Rendering' does not exist
```

`Tsudev.Audit.Core` build **thành công**. `Tsudev.Audit.Windows` cũng build được
— nghĩa là gói NuGet `System.Management` restore ổn, và `net8.0-windows`
biên dịch được trên Linux đúng như phán đoán.

**Quyết định & lý do:**

1. **Tập trung version vào `Directory.Build.props`**, xoá `<Version>`,
   `<Company>`, `<Product>`, `<Description>` khỏi `Cli.csproj`. Lý do: sắp tới
   sẽ có thêm installer và winget manifest cùng cần số hiệu phiên bản — nếu để
   rải rác thì chắc chắn có ngày lệch nhau.
2. **Chưa tách file gộp nhiều class thành file riêng.** Chỉ đổi chỗ file là đủ
   để build. Việc tách nhỏ là chỉnh trang, và làm nó trước khi có test xanh là
   tự chuốc rủi ro không cần thiết → dời sang Phase 4.
3. `RunAnalyzers=false` cho project test — bộ test dùng top-level statements,
   phân tích CA ở đó chỉ tạo nhiễu.

**Vấn đề đã gặp:** `dotnet sln add` từ chối `unittests.csproj` với lỗi
*"An XML comment cannot contain '--'"*. Nguyên nhân: comment trong csproj có
chuỗi `dotnet run --project`. Cách gỡ: viết lại comment không dùng dấu `--`.
Đây là bẫy dễ vấp khi viết comment chứa tham số dòng lệnh trong file XML.

**Nợ đã ghi nhận:** build Core sinh khoảng 25 cảnh báo CA1305 (ToString không
truyền IFormatProvider) và CA1826. Chưa sửa — sẽ xử lý gọn ở Phase 4 kèm
`.editorconfig`, vì CA1305 ở đây phần lớn vô hại nhưng vẫn nên chuẩn hoá.

**Tiếp theo:** Phase 2 — viết `HtmlReportRenderer`, `XlsxWriter`,
`DashboardBuilder`. Trước hết phải đọc kỹ `Program.cs` và `tests/unittests/Program.cs`
để trích ra **hợp đồng API chính xác** mà hai nơi này đang trông đợi.

---

### 13:30 — Phase 2 hoàn tất: viết mới lớp Rendering ✅

**Bối cảnh:** lớp `Core.Rendering` mất hoàn toàn, phải viết lại từ đầu sao cho
khớp chính xác các điểm gọi có sẵn trong `Program.cs` và bộ test.

**Cách làm:** không đoán. Trích hợp đồng API trực tiếp từ hai nơi gọi:
`Program.cs:86,87,118,145-146,153` và `tests/unittests/Program.cs:171-216`.
Bộ test mô tả hành vi khá chặt, coi như đặc tả sẵn có.

**Đã viết 3 file, tổng ~840 dòng:**

| File | Vai trò |
|---|---|
| `Rendering/HtmlReportRenderer.cs` | `Render(report, dashboardRelativeLink?)`; CSS nhúng, theme `#eaf2fc`, không dark-mode |
| `Rendering/XlsxWriter.cs` | `Write(path, tables)` + 3 hàm static `ColumnName`/`IsNumeric`/`MakeSafeSheetName` |
| `Rendering/DashboardBuilder.cs` | `BuildAndSave(level2Dir)`, quét đệ quy không giới hạn độ sâu |

**Kết quả: `54 PASS, 0 FAIL`** — khớp đúng con số 54 mà README tuyên bố. Việc
số test trùng khớp là bằng chứng tốt rằng API đã dựng lại đúng hợp đồng cũ.

`dotnet publish -r win-x64` chạy được từ Linux: `publish/tsudev-audit.exe`,
**34 MB**, `PE32+ executable (console) x86-64`.

**Quyết định & lý do:**

1. **XLSX tự ghi OOXML, không dùng ClosedXML/EPPlus** (theo quyết định đã chốt).
   Ghi chuỗi dạng `inlineStr` thay vì bảng `sharedStrings`: tốn dung lượng hơn
   chút nhưng bỏ hẳn một phần XML và một lớp đánh chỉ mục — ít chỗ sai hơn.
2. **`IsNumeric` chặt hơn `double.TryParse` có chủ đích.** `"007"` bị giữ
   nguyên là chuỗi. Trong báo cáo kiểm toán, chuỗi kiểu đó gần như luôn là mã
   số (phiên bản, mã sản phẩm, số sê-ri); biến thành số 7 là làm sai dữ liệu.
   Bộ test có sẵn ca kiểm thử cho đúng điểm này.
3. **`DashboardBuilder` bỏ qua lặng lẽ mọi file JSON hỏng.** Thư mục cấp 2 là
   nơi người dùng copy tay kết quả từ máy khác vào, chắc chắn sẽ có file lạ.
   Một file rác không được làm hỏng cả trang tổng hợp.
4. **Escape ở đúng một chỗ.** `HtmlReportRenderer.E()` là hàng rào duy nhất
   chống HTML injection. `DashboardBuilder` cần cột cuối là liên kết bấm được,
   nhưng thay vì nới lỏng escape, nó render xong rồi mới thay chuỗi **đã escape**
   bằng thẻ `<a>` do chính nó dựng. Hàng rào giữ nguyên.

**LỖI ĐÃ TỰ PHÁT HIỆN VÀ SỬA — đáng ghi lại:**

Ban đầu tôi đặt `<pane>` (đóng băng dòng tiêu đề) làm con trực tiếp của
`<worksheet>`, **sau** `</sheetData>`. Sai lược đồ SpreadsheetML: `<pane>` phải
nằm trong `<sheetViews><sheetView>`, và `<sheetViews>` phải đứng **trước**
`<cols>` và `<sheetData>`.

Vì sao đáng lưu ý: **file vẫn là XML hợp lệ**, vẫn mở được bằng thư viện đọc
dễ tính, nhưng **Excel sẽ báo file hỏng và đòi sửa**. LibreOffice thì bỏ qua.
Nghĩa là loại lỗi này **lọt lưới hoàn toàn nếu chỉ thử trên Linux**.

Cách phát hiện: viết một chương trình nhỏ ở scratchpad sinh file `.xlsx` thật,
rồi dùng `python3 zipfile` + `ElementTree` kiểm tra *thứ tự các phần tử con* của
`<worksheet>`, không chỉ kiểm tra "XML có hợp lệ không". Nay đã khẳng định:
`['sheetViews','cols','sheetData']` — đúng thứ tự lược đồ.

**Hạn chế của khâu kiểm chứng này — phải nói rõ:** môi trường không có `pip`,
không cài được `openpyxl`, không có LibreOffice. Nên file XLSX mới chỉ được
kiểm bằng phân tích XML thủ công, **chưa từng được Excel thật mở**. README cũ
tuyên bố "openpyxl + LibreOffice round-trip" — tuyên bố đó hiện **chưa kiểm
chứng lại được**. Cần đưa vào danh sách việc phải làm trên Windows.

**Tiếp theo:** đối chiếu với artifact kế hoạch mà người dùng vừa gửi.

---

### 14:00 — Người dùng gửi artifact kế hoạch, đối chiếu

**Bối cảnh:** giữa lúc đang viết lớp Rendering, người dùng gửi link artifact
"Kế hoạch tái cấu trúc toàn diện — tsudev SWICO" và yêu cầu đối chiếu.

**Đã làm:** đọc toàn văn artifact (61 KB, phải trích phần đuôi từ file HTML đã
lưu vì WebFetch cắt giữa chừng), lập `docs/DECISIONS.md`.

**Kết quả:**

1. **Chẩn đoán trùng khớp hoàn toàn.** Artifact và khảo sát độc lập ở phiên này
   ra cùng kết luận về mọi hạng mục chặn. Lập riêng rẽ mà trùng nhau → tăng độ
   tin cậy của cả hai. Ước lượng của artifact cho lớp Rendering (800–1.200 dòng)
   cũng sát thực tế (~840 dòng).

2. **⚠️ Mâu thuẫn nghiêm trọng về chữ ký số.** Artifact cảnh báo Azure Trusted
   Signing không dùng được từ Việt Nam. **Đã kiểm chứng độc lập — artifact đúng,
   và có thêm tin mới:** dịch vụ đã đổi tên thành **Azure Artifact Signing**,
   **bắt buộc Azure subscription trả phí**, danh sách quốc gia xác minh danh
   tính **không có Việt Nam**, và **không cấp chứng chỉ EV**.

**Quyết định & lý do:** không lấy kết luận của artifact ở dạng nguyên si, mà tra
lại tài liệu Microsoft hiện hành. Đây là quyết định tốn tiền và tốn thời gian
chờ — sai một lần là mất vài tuần. Kết quả: artifact đúng, nhưng thông tin của
nó cũng đã lạc hậu một phần (tên dịch vụ).

3. **Artifact có 9 hạng mục kế hoạch S001 bỏ sót**, đáng chú ý nhất: chốt một
   tên sản phẩm duy nhất, xử lý diệt virus báo nhầm, cân nhắc NativeAOT.

4. **Artifact xếp kiểm chứng Windows ở GĐ 02**, sớm hơn nhiều so với Phase 9 của
   kế hoạch S001. Artifact có lý hơn → đã sửa thứ tự.

---

### 14:20 — Chốt 4 + 3 quyết định với người dùng

| Mã | Quyết định |
|---|---|
| D1 | **SignPath Foundation** thay Azure → **buộc mã nguồn mở** |
| D2 | **Phạm vi chỉ CLI** — bỏ Agent (GĐ 06) và Server (GĐ 07) |
| D3 | **Có máy Windows thật**, người dùng tự chạy thử |
| D4 | Tên sản phẩm mới: **`tsuowlit SWICO`** |
| D5 | Đổi tên hiển thị + assembly (`swico.exe`); **giữ namespace** `Tsudev.Audit.*` |
| D6 | **Giữ tên miền `tsudev.com`** — không phải sửa test |
| D7 | Giấy phép **Apache-2.0** (có điều khoản bảo vệ nhãn hiệu) |

---

### 15:10 — Phase 3 hoàn tất: danh tính, pháp lý, đóng gói, CI/CD ✅

**Đã làm:**

- Đổi danh tính sản phẩm ở 4 chỗ: `Directory.Build.props`, `Cli.csproj`
  (`AssemblyName`), `app.manifest` (`assemblyIdentity`), `HtmlReportRenderer`
  (thêm hằng `ProductName`, giữ `BrandUrl` = tsudev.com). **54/54 test vẫn xanh.**
- Pháp lý: `LICENSE` (Apache-2.0 tải từ apache.org, điền chủ sở hữu), `NOTICE`,
  `THIRD-PARTY-NOTICES.md`, `EULA.txt`, `PRIVACY.md`.
- Đóng gói: `packaging/innosetup/swico.iss` (159 dòng), 3 manifest winget.
- CI/CD: `ci.yml`, `release.yml`.
- Tài liệu: `docs/SIGNING.md`, `docs/WINDOWS-VERIFICATION.md`, README viết lại,
  `CHANGELOG.md`, `build.ps1` cập nhật, `docs/PLAN.md` v2, `docs/STATE.md` v2.

**Quyết định & lý do đáng ghi lại:**

1. **`EULA.txt` mục 3 là mục quan trọng nhất của cả file.** Công cụ này đưa ra
   *phán quyết* về tính hợp lệ bản quyền của máy người dùng. Kết quả đó hoàn
   toàn có thể bị mang ra làm căn cứ trong tranh chấp lao động hoặc thanh tra.
   EULA phải nói thẳng: đây là dữ liệu kỹ thuật tham khảo, có thể sai theo cả
   hai chiều, và mọi quyết định dựa trên nó thuộc trách nhiệm người dùng.
2. **`AppId` trong Inno Setup không bao giờ được đổi.** Windows dựa vào GUID đó
   để nhận ra bản cũ khi nâng cấp. Đổi GUID = tạo ra sản phẩm thứ hai song song
   trên máy người dùng.
3. **Phiên bản truyền vào installer từ ngoài** (`/DAppVersion=`), không viết
   cứng trong `.iss`. Nguồn sự thật vẫn là `Directory.Build.props`.
4. **Ký exe TRƯỚC, đóng gói installer SAU** — để file exe nằm trong bộ cài là
   file đã có chữ ký.
5. **CI bắt buộc kiểm tra dấu thời gian**, không chỉ trạng thái chữ ký. Chữ ký
   thiếu dấu thời gian hết hiệu lực khi chứng chỉ hết hạn — lỗi chỉ lộ ra sau
   nhiều tháng nên phải chặn ngay tại CI.

**HAI LỖI ĐÃ TỰ PHÁT HIỆN VÀ SỬA trong `release.yml` trước khi commit:**

- **Ngữ cảnh `secrets` không dùng được trong `if:` ở cấp step** của GitHub
  Actions. Bản đầu tôi viết `if: ${{ env.SIGNPATH_API_TOKEN != '' }}` với biến
  khai báo ngay trong `env:` của chính step đó — không hoạt động. Đã sửa: quy
  đổi thành biến cấp job `SIGNPATH_CONFIGURED`.
- **SignPath đọc file cần ký từ GitHub artifact**, cần đúng `artifact-id` do
  `upload-artifact` trả về. Bản đầu tôi truyền `github.run_id` — sai. Đã thêm
  hai bước `upload-artifact` và tham chiếu `steps.*.outputs.artifact-id`.

**Tiếp theo — hai việc, cả hai đều cần người dùng, không phải tôi:**

1. Chạy `docs/WINDOWS-VERIFICATION.md` trên máy Windows thật.
   Ba mục then chốt: **B2** (`ThreatName`), **C3** (Excel mở `.xlsx`),
   **D1** (đối chiếu bản PowerShell cũ).
2. Nộp hồ sơ SignPath Foundation — **nộp sớm** vì duyệt mất vài tuần.

Sau khi có kết quả Phase 4, phiên tiếp theo làm Phase 5 (chuẩn hoá chất lượng
mã) dựa trên dữ liệu thật thay vì phỏng đoán.

---

### 15:40 — Người dùng vấp lỗi do tài liệu tôi viết sai

**Bối cảnh:** người dùng chạy `git clone https://github.com/tsuowlit/swico`
trong PowerShell (admin) trên Windows đúng theo hướng dẫn trong
`docs/WINDOWS-VERIFICATION.md`, và không tìm thấy repo.

**Nguyên nhân:** URL đó **do tôi tự đặt** khi chốt tên sản phẩm ở mục D4, dùng
làm chỗ điền tạm cho `RepositoryUrl`, manifest winget, `AppUrl` của installer,
`EULA.txt` mục 7 và `PRIVACY.md`. Nhưng khi viết `WINDOWS-VERIFICATION.md` tôi
lại mở đầu bằng `git clone <URL đó>` **như thể đó là địa chỉ có thật**. Repo
chưa từng tồn tại; `git remote -v` trống; 7 commit chỉ nằm trên máy dev này.

**Đã kiểm chứng thêm:** máy Linux này **không phải WSL** (`/proc/version` không
có "microsoft", `/mnt` trống, không có WSLInterop). Nghĩa là máy Windows của
người dùng là **máy tách biệt** — không với tới thư mục này qua `\\wsl$\` được.
Phải chuyển file thật.

**Sai lầm về trọng tâm, không chỉ về URL:** tôi mở đầu kịch bản kiểm chứng bằng
"clone rồi build", trong khi **đã publish sẵn `swico.exe` từ Linux**. Phần lớn
kịch bản — gồm cả ba mục then chốt B2, C3, D1 — chỉ cần **một file exe**, không
cần mã nguồn. Chỉ A1/A2 mới cần build trên Windows.

**Đã làm:**

- Tạo `dist/swico-portable.zip` (~29 MB) — copy sang Windows là chạy được ngay.
- Tạo `dist/swico-repo.bundle` (116 KB) — **một file** chứa nguyên vẹn cả 7
  commit và lịch sử. `git clone swico-repo.bundle swico` là ra kho đầy đủ.
  Dùng `git bundle` thay vì zip thư mục để sau này `git push` lên GitHub thật
  vẫn giữ đủ mọi mốc lùi.
- `dist/SHA256SUMS.txt` để đối chiếu sau khi copy qua USB/mạng.
- Sửa `WINDOWS-VERIFICATION.md`: cảnh báo rõ repo chưa tồn tại + 3 cách chuyển
  mã, xếp cách "chỉ copy exe" lên đầu vì đó mới là đường ngắn nhất.
- Thêm cảnh báo vào `README.md` (chưa phát hành) và mục cạm bẫy của `STATE.md`.
- `dist/` đưa vào `.gitignore`.

**Bài học ghi lại:** khi tự đặt một giá trị điền tạm (URL, ID, khoá), phải đánh
dấu nó là điền tạm **ở mọi nơi nó xuất hiện**, đặc biệt trong tài liệu hướng
dẫn thao tác. Một chỗ điền tạm nằm im trong file cấu hình thì vô hại; cũng chỗ
đó nằm trong câu lệnh bảo người dùng chạy thì thành lỗi.

**Tiếp theo:** không đổi — người dùng chạy kiểm chứng Windows (nay bằng
`dist/swico-portable.zip`) và nộp hồ sơ SignPath.

---

### 16:20 — Windows thật CHẠY ĐƯỢC + tạo kho mã trên GitHub

**Tin quan trọng nhất:** người dùng copy `swico-portable.zip` sang Windows, giải
nén, chạy `swico.exe` **thành công** và nhận được báo cáo HTML trên trình duyệt.

Đây là **lần đầu tiên `WindowsAdapters.cs` được thực thi** kể từ khi nó tồn tại.
Rủi ro số 1 của cả dự án — "adapter WMI chưa từng chạy thật" — nay đã giảm mạnh:
lớp adapter khởi động được, truy vấn được, và luồng dữ liệu chạy suốt tới khâu
render HTML.

**Chưa xong phần còn lại của Phase 4:** vẫn chưa đối chiếu *tên thuộc tính WMI*
(B2), chưa mở `.xlsx` bằng Excel thật (C3), chưa so với bản PowerShell cũ (D1).
Chạy được không đồng nghĩa dữ liệu đúng.

**Đã làm — kho mã:**

- Đổi 19 URL điền tạm ở 8 file sang `github.com/tsudev-tsudev/swico`.
  **Cố ý không sửa `docs/journal/`** — nhật ký là bản ghi lịch sử, sửa nó là làm
  sai sự thật về những gì đã xảy ra.
- Giữ nguyên `PackageIdentifier` winget là `tsuowlit.SWICO`: quy ước winget là
  `<NhàPhátHành>.<Gói>`, mà nhà phát hành vẫn là tsuowlit — `tsudev-tsudev` chỉ
  là tài khoản GitHub lưu trữ.
- Quét secret trước khi tạo repo: sạch.
- `gh repo create tsudev-tsudev/swico --private`, đổi nhánh `master` → `main`, push.
- **CI xanh cả hai job**, gồm cả smoke test chạy `swico.exe --help` trên Windows
  runner thật.

**Quyết định & lý do — repo để PRIVATE trước:** SignPath bắt buộc công khai,
nhưng biện pháp bù trừ (tách luật phát hiện) lúc đó **chưa làm**. Công khai ngay
nghĩa là phát tán 6 hạng mục dấu hiệu ở dạng chưa tách, tức là đúng cái giá mà
kế hoạch đã nêu — nhưng phải trả trước khi có biện pháp giảm nhẹ. Private trước
vẫn có đủ lợi ích quan trọng nhất: **bản sao lưu ngoài máy dev**.

---

### 17:00 — Phase 5a: tách luật phát hiện ✅

**Bối cảnh:** đây là điều kiện kỹ thuật để chuyển repo sang công khai, nên kéo
lên làm trước phần còn lại của Phase 5.

**Đã làm:**

- `Core/Rules/detection-rules.json` — toàn bộ dấu hiệu chuyển thành dữ liệu:
  `scanRoots`, `suspiciousNames`, `legitimateTaskNames`, `hookDirectories`,
  `hookFiles`, `knownKmsHosts`, `hostsInterferenceKeywords`, kèm `version`.
- `Core/Rules/DetectionRuleSet.cs` — nạp, kiểm tra tính hợp lệ, quay về an toàn.
- `ActivationRiskScanner` nhận `DetectionRuleSet`; **giữ nguyên chữ ký `Scan(ctx)`
  cũ** nên không nơi gọi nào phải sửa.
- CLI: `--rules <file.json>`, `--version`.
- Trường `DetectionRulesVersion` trong `AuditReport` (bổ sung, không phá JSON cũ).
- 10 test mới → **64 PASS, 0 FAIL**.
- `docs/DETECTION-RULES.md`.

**Quyết định & lý do:**

1. **Luật vẫn NHÚNG trong exe làm bậc dự phòng.** File ngoài chỉ để *ghi đè*.
   Lý do: công cụ phải chạy được khi người dùng chỉ copy mỗi một file exe —
   đúng kịch bản kỹ thuật viên cắm USB đi từng máy.
2. **Bộ luật RỖNG bị từ chối**, dù cú pháp hợp lệ. Bộ luật rỗng làm *mọi* máy
   đều "sạch" — nguy hiểm hơn bộ luật sai, vì nó tạo cảm giác an toàn giả mà
   không có dấu hiệu bất thường nào. Có test riêng cho điểm này.
3. **File luật hỏng không được làm hỏng lần quét.** Mọi lỗi nạp đều quay về bộ
   luật đóng kèm kèm cảnh báo hiện trong báo cáo.
4. **Có test chứng minh việc tách KHÔNG đổi hành vi**: `Scan(ctx)` và
   `Scan(ctx, Embedded)` phải cho kết quả y hệt. Refactor mà không có ràng buộc
   này thì chỉ là dịch chuyển rủi ro sang chỗ khác.
5. **Ghi `DetectionRulesVersion` vào báo cáo.** Khi luật cập nhật độc lập với
   exe, biết mỗi phiên bản exe là không đủ để truy ngược một kết luận.

**Phát hiện thêm:** phần trợ giúp `--help` vẫn còn tên cũ `tsudev System Audit`
và `tsudev-audit.exe` — sót từ đợt đổi tên. Đã sửa.

**Tiếp theo:** người dùng hoàn tất Phase 4 (B2, C3, D1) và quyết thời điểm
chuyển repo sang public để nộp SignPath.

---

### 17:40 — Phase 5b: dọn cảnh báo, phát hiện một lỗi thật ✅

**Đã làm:** dọn toàn bộ 27 cảnh báo trình phân tích, bật `TreatWarningsAsErrors`.

| Quy tắc | Cách xử lý |
|---|---|
| CA1305 (14) | Truyền `CultureInfo.InvariantCulture`. **Một trong số đó là lỗi thật** |
| CA1826 (8) | Thêm hàm mở rộng `FirstRowOrNull()` đọc theo chỉ số thay vì LINQ |
| CA1822 (3) | **Tắt có lý do** trong `.editorconfig` |
| CA1869 (1) | Đưa `JsonSerializerOptions` thành trường `static readonly` |
| CA1716 (1) | Đổi tên tham số `@namespace` → `wmiNamespace` |

**LỖI THẬT tìm được nhờ CA1305 — đáng ghi lại:**

`FileNaming.Stamp()` và `DateOnly()` dùng `ToString("yyyyMMdd")` **không truyền
`IFormatProvider`**. Trên máy đặt ngôn ngữ Thái (`th-TH`) hoặc Ả Rập (`ar-SA`),
lịch mặc định của .NET **không phải Gregorian** — `"yyyy"` cho ra **2569** thay
vì 2026.

Hậu quả nếu để nguyên: tên thư mục kết quả sai, quy ước 3 cấp vô hiệu, và trang
tổng hợp **không gom được kết quả giữa các máy đặt ngôn ngữ khác nhau** — đúng
kịch bản sử dụng chính của công cụ (kỹ thuật viên quét nhiều máy rồi gom lại).

Đây là lý do tôi **không tắt cả nhóm CA1305 cho nhanh**. Phần lớn cảnh báo
CA1305 trong dự án này đúng là vô hại, nhưng chúng che một cái không vô hại.
Nay CA1305 được **nâng lên mức lỗi** trong `.editorconfig` kèm ghi chú lý do.

**Quyết định & lý do — tắt CA1822 thay vì làm theo:** trình phân tích đề nghị
chuyển `HtmlReportRenderer.Render`, `XlsxWriter.Write`, `DashboardBuilder.Scan`
thành `static`. Nhưng cả `Program.cs` lẫn bộ test đều gọi qua đối tượng, và
`DashboardBuilder` nhận renderer qua hàm dựng. Chuyển sang static là phá API mà
không đổi lại lợi ích nào — đây là hàm chạy một lần mỗi báo cáo, không nằm
trong vòng lặp nóng.

**Quyết định & lý do — bật `TreatWarningsAsErrors` NGAY sau khi dọn:** để mở
thì cảnh báo tích tụ trở lại và không ai đọc nữa. Bật ngay lúc còn sạch là lúc
rẻ nhất.

**Đã đóng gói lại `dist/` cho người dùng:** bản portable nay kèm cả
`detection-rules.json` và `docs/DETECTION-RULES.md` để thử `--rules`.

**Tiếp theo:** không còn việc nào tôi làm được mà không cần người dùng. Hai việc
còn lại đều thuộc về người dùng — hoàn tất Phase 4 (B2, C3, D1) và quyết thời
điểm chuyển repo sang public để nộp SignPath.
