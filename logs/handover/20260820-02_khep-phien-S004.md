# PHIẾU BÀN GIAO — Khép phiên S004

- **Mã phiếu**: 20260820-02
- **Từ**: phiên S004 (Claude Code) — **Đến**: phiên sau / chủ project
- **Thời điểm**: 17:30 20/08/2026
- **Trạng thái**: HOÀN THÀNH — phiên S005 xử lý, 20/08/2026

> **Đọc theo thứ tự:** `AGENTS.md` → `docs/STATE.md` → phiếu này →
> `docs/journal/S004-2026-08-20.md` (diễn biến + **lý do** từng quyết định).

## 1. Việc đã làm xong

Các commit của phiên, **chưa push**:

| Commit | Nội dung |
|---|---|
| `bd8c7e0` | Áp bộ quy ước `tsudev-conventions` v1.0.0 (11 file), `.gitignore` hợp nhất, thêm `docs/ARCHITECTURE.md`, hoà giải hai hệ `STATE.md` và hai hệ ghi chép |
| `6d560d7` | **D-S004-1** — đổi quy ước đặt tên phiên bản sang `YY.M.DDNN` |
| `4584fb3` | Xoá `tsudev-conventions.zip` sau khi đối chiếu đủ 11 file |
| *(khép phiên)* | Phiếu bàn giao này + sửa tuyên bố sai ở `docs/STATE.md` mục 3.0 |

**Số liệu thật cuối phiên** (đo trên máy dev Linux, **chưa** qua runner GitHub):

```
Build : 0 cảnh báo, 0 lỗi
Test  : 264 PASS, 0 FAIL   (vào phiên: 234)
Cổng đặt tên: thử tay 10 chuỗi, đúng cả 10
```

**Một chỗ sai trong tài liệu đã sửa ở cuối phiên:** `docs/STATE.md` mục 3.0 và
khối trạng thái đang khẳng định commit của phiên **S003 chưa push** và "CI xanh"
đang nói về `ff1a9ae`. Kiểm bằng `git ls-remote origin refs/heads/main` →
`origin/main` = `200c0fd` (commit cuối của S003), và `gh run list` cho thấy CI
run **32224326731** cho chính commit đó **xanh**. Đã sửa cả hai chỗ. Nếu không
sửa, phiên sau sẽ đi push một thứ đã push rồi và tin vào một mốc CI sai.

## 2. Việc dang dở + bước tiếp theo CỤ THỂ

Hàng đợi đầy đủ: `logs/STATE.md`. Ba việc đầu, theo đúng thứ tự:

### 2.1 ⬜ Đẩy các commit của phiên lên `origin/main` — LÀM TRƯỚC TIÊN

```bash
git log --oneline origin/main..HEAD   # đếm bằng lệnh này, đừng tin số ghi cứng
git push origin main                   # SAU KHI người dùng đồng ý
```

> Bản thân phiếu này là một commit, nên mọi con số "còn N commit" viết trong
> phiếu đều sai ngay khi phiếu được commit. Đó là lý do dùng lệnh đếm.

⛔ Cần người dùng đồng ý: đẩy lên kho công khai là việc **hướng ra ngoài**.

Push xong **theo dõi CI ngay**, vì phiên này đổi đúng thứ mà cổng CI đang canh:

| Bước | Đã chạy thật chưa |
|---|---|
| `Kiem tra VersionPrefix…` (`ci.yml`) | Đã chạy **một lần** và xanh — nhưng với `VersionPrefix` **cũ** (`26.8.19`) và luật **cũ**. Luật mới + `26.8.1901` **chưa qua runner lần nào** |
| `Kiem tra quy uoc dat ten` (`release.yml`) | ⛔ **Chưa từng** — chỉ chạy khi gắn tag. CI xanh **không** chứng minh bước này chạy được |

Nếu job Linux đỏ ở bước kiểm `VersionPrefix`, xem `ci.yml` dòng 45–49: nó dùng
`sed` để rút `VersionPrefix` rồi gọi `dotnet run … -- --validate-release-name`.
Lệnh đó chạy đúng trên Linux khi thử tay.

### 2.2 ⛔ QU-2 — nhờ người dùng kiểm Inno Setup (rủi ro còn lại DUY NHẤT)

Mục **F7/F8** trong `docs/WINDOWS-VERIFICATION.md`. Trên máy có Inno Setup 6:

```powershell
iscc /DAppVersion=26.8.1901 packaging\innosetup\swico.iss   # F7
iscc /DAppVersion=26.9.0901 packaging\innosetup\swico.iss   # F8 - ngày MỘT chữ số
```

- **F7** kỳ vọng: không lỗi, file ra tên `tsudev-swico_26.8.1901_x64-setup.exe`.
- **F8** kỳ vọng: **không** báo lỗi số 0 đứng đầu ở `VersionInfoVersion`.

**Vì sao chưa biết:** máy dev là Linux, **không có `ISCC`**. Phía .NET đã đo
xong (MSBuild giữ `26.9.0901` ở `AssemblyInformationalVersion`, chuẩn hoá
`AssemblyVersion` thành `26.9.901`, bộ đọc nhận cả hai) — phía Inno Setup thì
**chưa ai chạy thử**.

**Nếu F8 đỏ:** không đổi lại quy ước. Truyền cho Inno Setup một `VersionInfoVersion`
đã bỏ số 0 (`26.9.901`) trong khi `OutputBaseFilename` vẫn giữ `26.9.0901` —
Inno chỉ dùng `VersionInfoVersion` cho metadata của file, không dùng để đặt tên.

### 2.3 ⛔ QU-3 — quyết có phát hành bản cầu nối không

`docs/STATE.md` mục 4.7 + `docs/VERSIONING.md` mục 5. Tóm tắt để khỏi phải mở:

- `swico.exe` của `26.8.18` và `26.8.18.2` **đã nằm trên máy người dùng** với bộ
  đọc phiên bản **cũ biên dịch sẵn bên trong**. Nó gặp tag `v26.8.1901` sẽ thấy
  ngày `1901 > 31` và không đọc được.
- Hệ quả **đã truy theo mã** (`UpdateChecker`): rơi vào nhánh `CheckFailed` →
  **vẫn quét bình thường kèm ghi chú**, không sập, không chặn — nhưng **mất khả
  năng cập nhật bắt buộc**.
- **Không sửa được bằng mã ở phía chúng ta.** Cách gỡ duy nhất: phát hành một
  **bản cầu nối** mang số hiệu **dạng cũ** (ví dụ `VersionPrefix` `26.8.20`,
  tag `v26.8.20`) chứa exe đã có bộ đọc hai dạng. Máy cũ đọc được tag đó → tự
  cập nhật → từ đó hiểu được cả dạng mới.

> ⚠️ Bản cầu nối đòi **tạm nới cổng chặn** (`ReleaseName.Validate` từ chối dạng
> cũ). **Đừng nới bằng cách sửa `Validate`** — luật sẽ ở lại vĩnh viễn. Thêm một
> biến môi trường `ALLOW_LEGACY_VERSION` cho đúng một lần chạy rồi bỏ đi.

## 3. File liên quan / đang khóa

**Không khóa file nào** — `logs/LOCKS.md` đã trả về trạng thái trống.

File phiên này chạm tới, để phiên sau biết chỗ mà tra:

| Vùng | File |
|---|---|
| Quy ước đặt tên | `Core/Updates/{VersionNumber,ReleaseName,ReleaseParsing}.cs` |
| Đóng gói | `Directory.Build.props`, `packaging/innosetup/swico.iss`, `packaging/winget/template/*.installer.yaml`, `packaging/tools/make-winget-manifest.py` |
| CI/CD | `.github/workflows/release.yml` (7 chỗ) |
| Test | `tests/unittests/Program.cs` mục 13, 13b, 15 |
| Tài liệu | `docs/VERSIONING.md` (viết lại), `README.md`, `docs/UPDATES.md`, `CHANGELOG.md`, `docs/STATE.md`, `docs/PLAN.md`, `docs/WINDOWS-VERIFICATION.md` |

## 4. Yêu cầu gửi agent đang giữ khóa

Không có.

## 5. Cảnh báo / quyết định quan trọng

### 5.1 Ba điều KHÔNG được "sửa cho gọn"

| Đừng động vào | Vì sao |
|---|---|
| `VersionNumber.TryParse` đọc **dạng cũ** | Hai bản đã phát hành ra ngoài. Bỏ đi là ngừng so sánh được với chính mình. Có test khoá lại. |
| `GitHubReleaseParser` nhận **cả hai** dạng tên tệp | Bỏ dạng cũ thì bản phát hành cũ bị coi là "không có file cài đặt" — hỏng **im lặng**. |
| Quy đổi **"bản thứ 01" → `Revision = 0`** | Bản lề khiến `26.8.18` ≡ `26.8.1801`. Đổi thành `Revision = 1` là làm hai bản đã phát hành thành phiên bản khác. |

### 5.2 Không đổi tên hai bản ĐÃ phát hành

`26.8.18` và `26.8.18.2` **giữ nguyên tên** trên trang phát hành. Đổi tên một bản
đã phát hành là làm hỏng mọi liên kết và mọi mã băm đã công bố về nó.
`docs/VERSIONING.md` mục 8 ghi rõ chúng thuộc dạng cũ.

### 5.3 `src/` không theo cây mẫu — có chủ đích

`PROJECT_STRUCTURE.md` mô tả cây `components|features|services|utils` (Web/Electron).
Repo này là .NET solution, `src/` chia theo project biên dịch. Lý do:
`docs/ARCHITECTURE.md` mục 3. **Không** tái cấu trúc — sẽ phá kiến trúc
Ports & Adapters vốn là thứ cho phép 264 test chạy được trên Linux.

### 5.4 Bản ghi lịch sử — cố ý KHÔNG sửa

`docs/journal/S001`–`S003`, `docs/DECISIONS.md`, các mục lịch sử trong `PLAN.md`
và `CHANGELOG.md`, cùng con số "234 test" trong phiếu `20260820-01`: đó là bản
ghi những gì đã đúng **tại thời điểm đó**. Quy tắc ở `docs/STATE.md` mục 4.6.

### 5.5 Bộ quy ước là bất khả xâm phạm

`AGENTS.md`, `docs/DESIGN_SYSTEM.md`, `docs/PROJECT_STRUCTURE.md`,
`docs/templates/HANDOVER.md`, `tokens/*` — chỉ đọc-hiểu-tuân thủ. Đã đối chiếu:
cả 7 file còn **nguyên bản từng byte**. `tsudev-conventions.zip` đã xoá; cách
dựng lại nguyên vẹn cho repo khác: `docs/CONVENTIONS-README.md`.

## 6. Kết quả xử lý (agent nhận điền sau khi thực hiện)

Phiên **S005** (20/08/2026) nhận phiếu này. Xử lý từng mục:

| Mục | Kết quả |
|---|---|
| **2.1** đẩy commit lên `origin/main` | ✅ **XONG.** Push sau khi chủ project đồng ý; `origin/main` `200c0fd` → `ce24691`. CI run **32360493509 xanh cả hai job** (Linux 25s, smoke test Windows 1m57s) |
| **2.2** QU-2 — Inno Setup F7/F8 | ⛔ **CHƯA** — cần máy có `ISCC`, máy dev là Linux. Vẫn nằm nguyên trong hàng đợi |
| **2.3** QU-3 — bản cầu nối | ⛔ **CHƯA** — là quyết định của chủ project, không phải việc kỹ thuật |

**Điều phiếu này nêu là "chưa ai xác nhận" thì nay đã xác nhận:** bước
`Kiem tra VersionPrefix dung quy uoc dat ten` trong `ci.yml` chạy được với luật
đặt tên **mới** và `VersionPrefix` `26.8.1901` trên runner GitHub thật.

**Điều vẫn đúng nguyên văn:** bước `Kiem tra quy uoc dat ten` trong `release.yml`
**chưa từng chạy thật** — chỉ kích hoạt khi gắn tag. Lần phát hành đầu theo quy
ước mới vẫn sẽ là lần chạy đầu tiên của bước đó.

**Mục 5 (ba điều không được "sửa cho gọn", hai bản đã phát hành giữ nguyên tên,
`src/` không theo cây mẫu, bản ghi lịch sử không sửa, bộ quy ước bất khả xâm
phạm) — S005 đã đọc và tuân thủ, không đụng vào chỗ nào trong đó.**

Phiếu tiếp theo: `logs/handover/20260820-03_khep-phien-S005.md`.
